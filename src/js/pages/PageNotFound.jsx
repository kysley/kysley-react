import React from 'react'

const PageNotFound = () => (
  <div className="container">
    <h1 className="fourohfour">It doesn't look like anything is here, friend :( </h1>
  </div>
)

export default PageNotFound
